---
name: run-animation
description: Run animation example with trajectory output
---

Run the animation example simulation and generate visualization:

```bash
scripts/build.sh &>/dev/null && cd example/animation && ./run.sh
```
