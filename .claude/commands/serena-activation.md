---
name: serena-activation
description: Activate Serena project in current directory
---

Call the `mcp__serena__activate_project` tool with the project parameter
set to the value of the CLAUDE_PROJECT_DIR environment variable.

After activation, respond with only: "Project activated at <project_path>" where <project_path> is the activated project directory.
